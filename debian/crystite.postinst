#!/bin/sh

set -e

#DEBHELPER#

case "${1}" in
  configure)
    chown root:neosvr /etc/crystite/appsettings.json
    chmod 640 /etc/crystite/appsettings.json

    chown neosvr:neosvr /var/log/neosvr
    ;;
  *)
    ;;
esac
